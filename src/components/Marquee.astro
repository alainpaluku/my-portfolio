---
import { useTranslations, type Lang, defaultLang } from '../i18n/translations';

interface Props {
  lang?: Lang;
}

const { lang = defaultLang } = Astro.props;
const t = useTranslations(lang);
const items = t.marquee;
const colors = ['text-accent', 'text-secondary', 'text-primary'];
---

<div class="w-full overflow-hidden relative z-20 py-6 sm:py-8">
  <div class="bg-black/50 border-y border-white/10 py-3 sm:py-4 -rotate-2 -mx-4 px-4">
    <div class="flex whitespace-nowrap animate-marquee">
      {[...items, ...items].map((item, i) => (
        <span class="text-base sm:text-xl font-mono text-gray-400 mx-4 sm:mx-8 flex items-center gap-2">
          <span class={colors[i % colors.length]}>+</span> {item}
        </span>
      ))}
    </div>
  </div>
</div>
