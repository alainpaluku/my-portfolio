---
interface Props {
  filter: string;
  label: string;
  isActive?: boolean;
}

const { filter, label, isActive = false } = Astro.props;
---

<button 
  data-filter={filter}
  class={`filter-btn ${isActive ? 'active' : ''}`}
  aria-pressed={isActive}
>
  {label}
</button>
