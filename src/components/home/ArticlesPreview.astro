---
import { useTranslations, type Lang, defaultLang } from '../../i18n/translations';
import SectionPreview from '../base/SectionPreview.astro';
import { articles } from '../../data/articles';
import { getLocalizedPath } from '../../utils/routing';

interface Props {
  lang?: Lang;
}

const { lang = defaultLang } = Astro.props;
const t = useTranslations(lang);
---

<SectionPreview
  lang={lang}
  title={t.articles.title}
  subtitle={t.articles.subtitle}
  items={articles}
  cardType="article"
  viewAllText={t.articles.viewAll}
  viewAllHref={getLocalizedPath('/articles', lang)}
  itemsToShow={3}
/>
